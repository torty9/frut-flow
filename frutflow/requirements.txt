# Core (everything runs locally / on-device)
#
# DEFAULT engine is Parakeet via MLX (runs on the Apple-Silicon GPU): ~6-12x faster
# than faster-whisper on this hardware AND more accurate, with punctuation built in.
parakeet-mlx>=0.5.2            # NVIDIA Parakeet TDT on Apple MLX (default backend)
mlx>=0.31                     # Apple MLX runtime (Metal GPU); pulls mlx-metal
faster-whisper>=1.0.2          # fallback backend ("local"); >=1.0.2 for `hotwords`
sounddevice>=0.4.6
pynput>=1.7.6
numpy>=1.24
pyobjc-framework-AVFoundation>=10.0   # to request the macOS microphone prompt
pyobjc-framework-Cocoa>=10.0          # NSWorkspace + AX reads + in-process NSPasteboard

# Accuracy layer — phonetic + fuzzy proper-noun correction and auto-learning.
# Small, pure-wheel installs. If missing, the app still runs (those features just
# no-op), but you want them: this is what fixes "Versal" -> "Vercel" automatically.
rapidfuzz>=3.0
jellyfish>=1.0

# Optional on-device cleanup (fully local — no cloud, no API key):
#   cleanup = "local"  ->  pip install "mlx-lm>=0.28" "transformers>=4.44,<5"
# Conservative, context-aware repair of MISHEARD words (homophones etc.) using a small
# MLX instruct model that reuses the already-installed mlx runtime + shared HF cache.
# The ~0.9 GB repair model downloads automatically on first use.
# IMPORTANT: mlx-lm's metadata asks for transformers>=5, but 5.x BREAKS `import mlx_lm`
# (incompatible tokenizer registration). Pin transformers<5 — verified 4.57.6; Parakeet
# works on 4.x too. That pin conflicts with mlx-lm's declared metadata, so install the
# two together with the explicit command above rather than expecting the resolver to.
# mlx-lm>=0.28
# transformers>=4.44,<5
